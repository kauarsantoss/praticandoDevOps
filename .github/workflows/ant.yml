# This workflow will build a Java project with Ant
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-ant

name: Java CI

on:
  push:
    branches: [ "dev" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 20
      uses: actions/setup-java@v3
      with:
        java-version: '20'
        distribution: 'oracle'
    - name: Build with Ant
      run: javac src/ogro.java -d out

    - name: Criar arquivo de input
      run: echo "2\n4" > input.txt

    - name: Rodando Programa
      run: java -cp out ogro

    - name: mensagem de sucesso
      run: echo DEU GREEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEENNNNNNNNNNNNNNNNNNNN!!!!!!!!!!!!!!!!!!!
